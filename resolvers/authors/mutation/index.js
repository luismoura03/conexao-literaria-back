const { createAuthor } = require("./create");
const { deleteAuthor } = require("./delete");
const { updateAuthor } = require("./update");

module.exports = { createAuthor, deleteAuthor, updateAuthor };
